import React from 'react';
import {ITestProps} from "../../helpers/interfaces";

const TestCard = (props: ITestProps) => {
    return (
        <div>
            Hey I'm test
        </div>
    );
};

export default TestCard;
